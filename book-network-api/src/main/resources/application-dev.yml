spring:
  datasource:
    #url: jdbc:postgresql://141.95.1.65:5432/book_social_network
    url: jdbc:postgresql://localhost:5432/book_social_network
    username: username
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
      #ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    #host: ${EMAIL_HOSTNAME}
    host: smtp.gmail.com
    port: 465
    #username: ${EMAIL_USERNAME}
    username: sonnyhardy265@gmail.com
    #password: ${EMAIL_PASSWORD}
    password: mtthoalxficomxzj
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            trust: "*"
        starttls:
          enable: true
    protocol: smtp

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/book-social-network
          jwk-set-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

application:
  cors:
    origins: http://localhost:4200,http://localhost:8083,http://141.95.1.65:8083
  file:
    upload:
      photos-output-path: ./uploads

server:
  port: 8082